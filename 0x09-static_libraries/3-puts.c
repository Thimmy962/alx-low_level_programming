#include "main.h"
#include <stdio.h>

/**
 * _puts - ceck te code
 * @str: takes a sting as input
 * description: prints to the standard output
 * Return: Noting
*/

void _puts(char *str)
{
	puts(str);
}
