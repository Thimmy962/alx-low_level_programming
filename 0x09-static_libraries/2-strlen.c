#include "main.h"
#include <string.h>
/**
 * _strlen - check the description
 * @s: function takes a string a input
 * description: Count the characters in a string
 * Return: Returns the length of a string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
