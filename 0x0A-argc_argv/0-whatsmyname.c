#include "main.h"
#include <stdio.h>

/**
 * main - prints its name
 * @argc: command line argument
 * @argv: command line argument
 * Return: int
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

