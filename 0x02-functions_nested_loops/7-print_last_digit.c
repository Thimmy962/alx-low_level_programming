#include "main.h"

/**
 * _print_last_digit - Check the code
 * @i: An input interger
 * Description: returns the last number in an integer
 */



int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (n * -1);
}
