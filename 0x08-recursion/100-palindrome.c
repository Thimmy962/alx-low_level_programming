#include "main.h"
#include <string.h>

/**
 * i_palindrome - check if a string is a palindrome
 * @s: takes a string ss as an input
 * Return: int
*/

int is_palindrome(char *s)
{
	int a = strlen(s);
	if (s != '\0')
	{

	}
}