#include "main.h"
#include <string.h>

/**
 * _strcmp - check description
 * @s1: takes a string as input
 * @s2: takes a string as input
 * description: compares two strings
 * Return: returns (0)
*/

int _strcmp(char *s1, char *s2)
{
	strcat(s1, s2);
	return (0);
}
